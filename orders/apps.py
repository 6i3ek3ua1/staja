from django.apps import AppConfig


class OrdersConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'orders'

    def ready(self):
        from orders.tasks import initiate_auto_payments
        initiate_auto_payments.delay()
